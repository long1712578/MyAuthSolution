name: CI Pipeline

on:
  push:
    branches: master
  pull_request:
    branches: master

jobs:
  build_authserver:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'

    - name: Restore OAuthOpenIdSolution dependencies
      run: dotnet restore ./OAuthOpenIdSolution/OAuthOpenIdSolution.sln

    - name: Build
      run: dotnet build ./OAuthOpenIdSolution/OAuthOpenIdSolution.sln --no-restore --configuration Release

    - name: Test
      run: dotnet test ./OAuthOpenIdSolution/OAuthOpenIdSolution.sln --no-build --verbosity normal
  build_protectedapi:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore ProtectedApi dependencies
        run: dotnet restore ./OAuthOpenIdSolution/ProtectedApi/ProtectedApi.csproj

      - name: Build ProtectedApi
        run: dotnet build ./OAuthOpenIdSolution/ProtectedApi/ProtectedApi.csproj --no-restore --configuration Release

      - name: Test ProtectedApi
        run: dotnet test ./OAuthOpenIdSolution/ProtectedApi/ProtectedApi.csproj --no-build --verbosity normal

